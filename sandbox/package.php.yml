version: dev
description: The jphp Cryptography sandbox

plugins:
  - App

sources:
  - src

tasks:
  start-upd:
    depends-on: ['install -f', 'start']

includes:
  - app.php
deps:
  jphp-core: '^1.0.0'
  jphp-crypton: '*'